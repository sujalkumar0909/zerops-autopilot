zerops:
  - setup: api
    build:
      base: nodejs@22
      buildCommands:
        - cd backend && npm install
      deployFiles:
        - ./backend
    run:
      base: nodejs@22
      start: cd backend && npm start